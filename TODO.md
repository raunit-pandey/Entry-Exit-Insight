# EntryExit Insight UI Upgrade TODO

## Plan Breakdown
1. **[ ]** Create new CSS block for theme toggle, premium tabs, and animations (add to app.py).
2. **[ ]** Replace header theme toggle button with animated pill switch HTML.
3. **[ ]** Upgrade tabs with premium pill styling wrapper.
4. **[ ]** Apply visual polish (transitions, spacing).
5. **[ ]** Test and verify changes (manual streamlit run).
6. **[ ]** Mark complete.

**Instructions**: Update this file after each step completion by checking the box `[x]` and adding notes.
</xai:function_call > 

**TODO.md created with step-by-step plan.** Proceeding to Step 1: Add new CSS block. First, need to identify exact insertion point by searching for end of existing CSS in app.py.

<xai:function_call name="search_files">
<parameter name="path">.
